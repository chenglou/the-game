var React = require('react');
var UnitWrapper = require('./UnitWrapper');

var Tombstone = React.createClass({
  render: function() {
    return (
      <UnitWrapper unitName="Tombstone">
      </UnitWrapper>
    );
  }
});

module.exports = Tombstone;
