var React = require('react');
var UnitWrapper = require('./UnitWrapper');

var Tree = React.createClass({
  render: function() {
    return (
      <UnitWrapper unitName="Tree">
      </UnitWrapper>
    );
  }
});

module.exports = Tree;
