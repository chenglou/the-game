var React = require('react');
var UnitWrapper = require('./UnitWrapper');

var Grass = React.createClass({
  render: function() {
    return (
      <UnitWrapper unitName="Grass">
      </UnitWrapper>
    );
  }
});

module.exports = Grass;
